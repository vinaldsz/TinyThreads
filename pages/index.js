export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>TinyThreads</h1>
      <p>Welcome — this is the Next + Express scaffold.</p>
      <p>
        Try the API: <code>/api/hello</code>
      </p>
    </main>
  );
}
